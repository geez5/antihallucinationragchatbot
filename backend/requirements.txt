# ==============================================================================
# requirements.txt — Veritas RAG Chatbot Backend
# Minimal direct dependencies only — let pip resolve transitive deps on Linux.
# ==============================================================================

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.30.6
python-dotenv==1.0.1

# Rate limiting
slowapi==0.1.9

# LLM clients
groq==0.26.0
google-generativeai==0.8.3

# Vector store
chromadb==0.5.23

# Reranker (Jina REST API — no extra package needed, uses requests)
requests==2.32.3

# HTTP
httpx==0.27.2

# Data validation
pydantic==2.9.2

# Web scraping (crawler.py)
beautifulsoup4==4.12.3
lxml==5.3.0
